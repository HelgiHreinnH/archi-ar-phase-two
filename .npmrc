# three@0.170 vs @google/model-viewer@4.3, which peers three@^0.183. Three's
# version is pinned by MindAR's expectations, so this stays until the Phase 4
# framework decision. (Lovable used bun, which is lenient; npm is not.)
legacy-peer-deps=true
